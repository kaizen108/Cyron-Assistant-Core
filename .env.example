# Discord bot (required for bot service)
DISCORD_TOKEN=

# Discord OAuth (required for dashboard login)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Shared secret between bot and backend (generate a long random string)
BOT_API_KEY=

# JWT signing secret (use 32+ random bytes in production)
AUTH_JWT_SECRET=

# AI provider — at least one is required
OPENAI_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini/gemini-2.0-flash-lite

# Production URLs
FRONTEND_ALLOWED_ORIGINS=https://cyron-assistant-frontend.vercel.app,http://localhost:5173
BACKEND_PUBLIC_URL=https://api.cyronticket.com
DISCORD_OAUTH_ALLOWED_REDIRECT_URIS=https://cyron-assistant-frontend.vercel.app/auth/callback,http://localhost:5173/auth/callback
FRONTEND_PUBLIC_URL=https://cyron-assistant-frontend.vercel.app

# Database and cache (Docker defaults shown; override for managed services)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/ai_ticket_assistant
REDIS_URL=redis://redis:6379/0
POSTGRES_PASSWORD=postgres

# Server
HOST=0.0.0.0
PORT=8000
RELOAD=false
LOG_LEVEL=INFO
